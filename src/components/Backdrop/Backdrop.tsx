import React from 'react';

const Backdrop: React.FC = () => {
    return (
        <div
            className="modal-backdrop show"
            style={{display: 'block'}}
        />
    );
};

export default Backdrop;